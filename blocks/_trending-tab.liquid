{% liquid
  assign desktop_limit = block.settings.product_limit | default: 6
  assign mobile_limit = block.settings.product_limit_mobile | default: 4
  assign product_limit = desktop_limit
  if mobile_limit > desktop_limit
    assign product_limit = mobile_limit
  endif
  assign tab_label = block.settings.tab_label | default: block.settings.collection.title | default: 'Collection'
  assign carousel_ref = 'trendingCarousel' | append: block.id
%}

<div
  class="trending__panel"
  data-tab-label="{{ tab_label | escape }}"
  data-trending-panel
  {{ block.shopify_attributes }}
>
  {% capture list_items %}
    {% if block.settings.collection != blank %}
      {% for product in block.settings.collection.products limit: product_limit %}
        <div class="resource-list__item{% if forloop.index > desktop_limit %} trending-product--hide-desktop{% endif %}{% if forloop.index > mobile_limit %} trending-product--hide-mobile{% endif %}">
          {% content_for 'block',
            type: '_product-card',
            id: 'static-product-card',
            closest.product: product,
            product_view_context: 'collection'
          %}
        </div>
        {% unless forloop.last %}
          <!--@list/split-->
        {% endunless %}
      {% endfor %}
    {% endif %}
  {% endcapture %}

  {% liquid
    assign list_items_array = list_items | strip | split: '<!--@list/split-->'
    assign slide_count = list_items_array.size
  %}

  {% render 'resource-list',
    list_items: list_items,
    list_items_array: list_items_array,
    settings: section.settings,
    carousel_ref: carousel_ref,
    slide_count: slide_count,
    content_type: 'products',
    test_id: 'trending-resource-list'
  %}

  {% if block.settings.collection != blank and section.settings.view_all_label != blank %}
    <div class="trending__view-all" data-trending-view-all>
      <a class="button button--primary" href="{{ block.settings.collection.url }}">
        {{ section.settings.view_all_label | default: 'View all' }}
        <svg
          viewBox="0 0 24 24"
          width="14"
          height="14"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          aria-hidden="true"
        >
          <path d="M5 12h14M13 6l6 6-6 6" />
        </svg>
      </a>
    </div>
  {% endif %}
</div>

{% stylesheet %}
  @media screen and (max-width: 989px) {
    .resource-list__item.trending-product--hide-mobile,
    slideshow-slide:has(.trending-product--hide-mobile) {
      display: none;
    }
  }

  @media screen and (min-width: 990px) {
    .resource-list__item.trending-product--hide-desktop,
    slideshow-slide:has(.trending-product--hide-desktop) {
      display: none;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Collection tab",
  "tag": null,
  "settings": [
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    },
    {
      "type": "text",
      "id": "tab_label",
      "label": "Tab label (optional — defaults to collection name)"
    },
    {
      "type": "range",
      "id": "product_limit",
      "label": "t:settings.products_to_show_desktop",
      "min": 2,
      "max": 12,
      "step": 1,
      "default": 6
    },
    {
      "type": "range",
      "id": "product_limit_mobile",
      "label": "t:settings.products_to_show_mobile",
      "min": 2,
      "max": 12,
      "step": 1,
      "default": 4
    }
  ]
}
{% endschema %}